from neural_network import Model 
import numpy

def load_and_predict(): 
    #load the model and use the predict method to output prediction. Currently not training correctly so just a placeholder function at the minute.
    return numpy.random.choice(list('ABCDEFGHIJKLMNOPQRSTUVWXYZ'))